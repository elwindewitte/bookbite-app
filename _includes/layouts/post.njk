---
layout: layouts/base.njk
---
<h2>{{ title }}</h2>
<img src="{{ featuredImage | url }}" alt="{{ featuredImage }}"></img><!--Using the 'url' filter to get a properly formatted url for the src-->
<p>{{ content | safe }}</p><!--This referres to the markdown content which is not meta data-->